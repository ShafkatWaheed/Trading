__pycache__/
*.pyc
*.pyo
.env
trading.db
trading.db-shm
trading.db-wal
trading.db-journal
reports/*.pdf
reports/*.html
reports/*.json
.venv/
.claude/settings.local.json

# Node / Next.js
node_modules/
.next/
*.tsbuildinfo

# Logs / OS / editors
*.log
.DS_Store
.idea/
.vscode/

# Sensitive runtime artifacts
session_qr.png
session_qr.*

# Database backups (large binaries, never commit)
*.backup
*.bak
trading.db.backup-*

# Fetched data caches — regenerated on demand by the loaders
cache/

# Local DB backups (kept 14 days, ~1.4GB at full retention)
data/db_backups/

# Daily digest files (rebuildable from DB)
data/digests/

# AI analyst playbook — evolves at runtime (auto-created from template)
data/predictions/analyst_playbook.md
